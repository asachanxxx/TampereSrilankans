# Supabase Configuration
# Get these values from your Supabase project settings
# https://supabase.com/dashboard/project/_/settings/api

# Public URL - Safe to expose to the browser
NEXT_PUBLIC_SUPABASE_URL=your-project-url.supabase.co

# Anonymous/Public Key - Safe to expose to the browser
NEXT_PUBLIC_SUPABASE_ANON_KEY=your-anon-key-here

# Service Role Key - NEVER expose to the browser
# Only use server-side for admin operations that bypass RLS
# Keep this secret and secure!
SUPABASE_SERVICE_ROLE_KEY=your-service-role-key-here

# Site URL - Used for OAuth redirects
NEXT_PUBLIC_SITE_URL=http://localhost:3000

# Admin Email Allowlist
# Comma-separated list of emails that will be auto-assigned admin role on first login
# Example: admin@example.com,owner@example.com
ADMIN_EMAILS=admin@example.com

# Gmail Email Service
# Used to send ticket links to registrants
# GMAIL_USER: your organisation Gmail address
# GMAIL_APP_PASSWORD: generate at myaccount.google.com → Security → App Passwords
#   (requires 2FA enabled on the Gmail account)
GMAIL_USER=yourorg@gmail.com
GMAIL_APP_PASSWORD=xxxx xxxx xxxx xxxx

# Application URL - Used to generate ticket links in emails
# In production, set this to your actual domain (e.g., https://tamperesrilankans.fi)
NEXT_PUBLIC_APP_URL=http://localhost:3000
